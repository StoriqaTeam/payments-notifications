kind: Service
apiVersion: v1
metadata:
  name: payments-notifications
  labels:
    stack: payments
spec:
  selector:
    stack: payments
    app: notifications
  ports:
  - name: main
    protocol: TCP
    port: 8000
    targetPort: 8000
